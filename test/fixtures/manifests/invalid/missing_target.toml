[blink]
version = "1"

[targets.local]
type = "local"

[services.broken.source]
type = "github_release"
repo = "Bare-Systems/Blink"
asset = "blink-linux-amd64"

[services.broken.deploy]
target = "prod"
pipeline = ["fetch_artifact", "verify"]

[services.broken.verify]
suite = "suite.rb"
